# LocalFileSharing
局域网文件互传

## 使用方式
1. 确保本地已安装 Node.js
2. 克隆本项目到本地
3. 打开项目目录，运行 `run.bat`，会自动编译
4. 打开浏览器，本机访问 `http://localhost:8800`，其它设备访问 `http://<本机IP>:8800`

代码都是AI写的，仅供参考。
只做了基本的小文件测试，未进行大文件测试。
